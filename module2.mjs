import ds ,{a,c,d} from './module1.mjs'
// we can write any name except ds if we want;
console.log(ds);
// if we are importing like this we have to give the same name as it is having in import section.
console.log(a);
console.log(c);
console.log(d);